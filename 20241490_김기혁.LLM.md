#  기존 프롬프트

PROMPT_TEMPLATE = 'You are a web security expert. Classify each HTTP request as "Normal" or "Anomalous" and provide a brief reason.\n\nExamples:\nRequest: GET /index.jsp HTTP/1.1\nOutput: {{"label": "Normal", "reason": "Standard page request, no suspicious pattern"}}\n\nRequest: GET /search?q=\' OR \'1\'=\'1 HTTP/1.1\nOutput: {{"label": "Anomalous", "reason": "Classic SQL Injection pattern with OR 1=1"}}\n\nNow classify:\nRequest: {http_text}\nOutput:'


# 시간

  10/100건 완료 (12.3초, 건당 1.23초)
  20/100건 완료 (24.3초, 건당 1.21초)
  30/100건 완료 (33.8초, 건당 1.13초)
  40/100건 완료 (45.8초, 건당 1.14초)
  50/100건 완료 (57.9초, 건당 1.16초)
  60/100건 완료 (69.7초, 건당 1.16초)
  70/100건 완료 (80.2초, 건당 1.15초)
  80/100건 완료 (92.3초, 건당 1.15초)
  90/100건 완료 (103.2초, 건당 1.15초)
  100/100건 완료 (114.3초, 건당 1.14초)

총 소요: 114.3초
1만 건 환산: 약 191분

# 정확도

LLM 정확도: 0.8400
LLM F1:    0.8298
분류 실패(Unknown): 1건

              precision    recall  f1-score   support

      Normal       0.90      0.80      0.85        56
   Anomalous       0.78      0.89      0.83        44

    accuracy                           0.84       100
   macro avg       0.84      0.84      0.84       100
weighted avg       0.85      0.84      0.84       100


# 수정 프롬프트 

PROMPT_TEMPLATE = """You are a highly skilled Cyber Security Analyst specializing in Web Application Firewall (WAF) log analysis. 
Your task is to classify HTTP requests as "Normal" or "Anomalous".

Guidelines:
1. "Normal": Standard user behavior, legitimate parameters, and common web paths.
2. "Anomalous": SQL injection, XSS, Path Traversal, Command Injection, or suspicious automated scanning.

Examples:
Request: GET /images/logo.png HTTP/1.1
Output: {{"label": "Normal", "reason": "Static resource request with no parameters."}}

Request: POST /login.jsp HTTP/1.1
Body: user=admin&pass=1234
Output: {{"label": "Normal", "reason": "Standard login attempt with typical form data."}}

Request: GET /products.php?id=10 AND 1=1 HTTP/1.1
Output: {{"label": "Anomalous", "reason": "SQL Injection: Boolean-based blind attack pattern detected in 'id' parameter."}}

Request: GET /etc/passwd HTTP/1.1
Output: {{"label": "Anomalous", "reason": "Path Traversal: Attempt to access sensitive system files."}}

Request: GET /search?q=<script>alert(1)</script> HTTP/1.1
Output: {{"label": "Anomalous", "reason": "Cross-Site Scripting (XSS): Script tag detected in query parameter."}}

Now, analyze the following request and respond strictly in JSON format.
Request: {http_text}
Output:"""

# 시간

  10/100건 완료 (7.9초, 건당 0.79초)
  20/100건 완료 (15.1초, 건당 0.75초)
  30/100건 완료 (23.0초, 건당 0.77초)
  40/100건 완료 (30.9초, 건당 0.77초)
  50/100건 완료 (38.6초, 건당 0.77초)
  60/100건 완료 (46.8초, 건당 0.78초)
  70/100건 완료 (53.5초, 건당 0.76초)
  80/100건 완료 (61.5초, 건당 0.77초)
  90/100건 완료 (68.4초, 건당 0.76초)
  100/100건 완료 (75.3초, 건당 0.75초)

총 소요: 75.3초
1만 건 환산: 약 125분

# 정확도 

LLM 정확도: 0.6200
LLM F1:    0.2692
분류 실패(Unknown): 69건

              precision    recall  f1-score   support

      Normal       0.60      0.98      0.74        56
   Anomalous       0.88      0.16      0.27        44

    accuracy                           0.62       100
   macro avg       0.74      0.57      0.51       100
weighted avg       0.72      0.62      0.53       100


# 비교 

"지시사항이 복잡할수록 소형 모델은 무너진다"

기존 프롬프트 (0.84): 심플한 지시로 모델이 정답 형식(JSON)을 완벽히 지킴.

수정 프롬프트 (0.62): 상세한 가이드라인을 주었으나, 모델이 이를 처리하다 출력 형식을 깨뜨림.

결과: 수정 프롬프트는 100건 중 69건을 판단하지 못해(Unknown) 보안 모델로서의 기능을 상실함.

# 원인

1. 모델의 수용량 초과 (Model Capacity)

우리가 사용하는 gemma3:4b는 모델 크기가 작음.

기존: "이거 보고 분류해"라고 하니 분류에만 집중함.

수정: "너는 보안 전문가야, 가이드는 이렇고, 예시는 저거야, JSON으로만 해"라고 하니 모델이 과부하에 걸려 JSON 괄호를 빼먹거나 설명글을 덧붙임.

2. Unknown 처리 방식의 함정

수정본에서 발생한 69건의 실패가 코드 로직상 모두 '정상(Normal)'으로 분류됨.

우연히 테스트 데이터에 '정상'이 많아서 정확도가 0.62라도 나온 것이지, 실제로는 모델이 아무것도 판단하지 못한 상태.